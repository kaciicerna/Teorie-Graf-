#2a)
    # minimalni kostra, prima cesta - uplny -> to ne, potrebuji souvisly graf,
    # nesmi vzniknout smycka
    # minimalni cesta - je dobre si oznacovat ty co jsou propojenen, pokud x=0 nebo x=1 pridam znacku, pokud ne vynecham, 
        #tohle je v cyklu vcetne ukoncovaci podminky (while... dokud jsou propojeny vsechny uzly, kdyz se projdou vsechny hrany)
        #dulezite osetrit vsechny stavy
#2b)
    #artikulace a mosty (viz prednaska) 
#2c)
    # nejkratsi cesta - ze sekce mustek do unikovy_modul
    # prochazeni grafu (sirka a hloubka) - nezname bludiste
    # vysledky jsou u kazdeho jine, dat si pozor na vysledky (dlouhy vysledek - projit vsechny uzly)

#2d)
    #AVL strom - predek a naslednici